# ContextInput 0.2.1 beta

This is a signing and update-continuity test release. It intentionally contains
no language-selection behavior changes from 0.2.0.

- Verifies an in-place Sparkle update from Build 15 to Build 16.
- Retains the same bundle identifier, beta signing certificate, and update key so
  macOS Accessibility approval should remain enabled after relaunch.
